{
  "name": "api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.5.0",
    "@tentacle-pro/core": "workspace:*",
    "@tentacle-pro/db": "workspace:*",
    "@tentacle-pro/render": "workspace:*",
    "@tentacle-pro/wechat": "workspace:*",
    "hono": "^4.12.4",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}