# 迁移执行计划：woa-layout → backend-tentacle-pro

> 状态说明：⬜ 待执行 / 🔄 进行中 / ✅ 已完成 / 🧪 等待确认

---

## 阶段总览

| 阶段 | 内容 | 等待确认 |
|------|------|---------|
| M1 | packages/render — 迁入渲染引擎 | 是 |
| M2 | packages/db — 扩展模板/素材表 | 是 |
| M3 | api/ — 接入真实渲染，替换 stub | 是 |
| M4 | packages/ui — 迁入编辑器 React 组件 | 是 |
| M5 | web/ — 首页 + Dashboard Shell + 编辑器页 | 是 |

---

## M1：packages/render（渲染引擎）

**来源**：`woa-layout/src/lib/converter/`

### 任务列表

- ⬜ **M1-1** 创建 `packages/render/package.json`，声明 unified 生态依赖
- ⬜ **M1-2** 迁移 `markdown-to-html.ts`（核心 unified pipeline）
- ⬜ **M1-3** 迁移 `tailwind-to-inline.ts`（Tailwind → 内联样式）
- ⬜ **M1-4** 迁移 `rehype-inject-styles.ts`（HAST 样式注入插件）
- ⬜ **M1-5** 迁移 `rehype-wechat-enhancements.ts`（Mermaid/LaTeX/链接引用）
- ⬜ **M1-6** 输出 `packages/render/src/index.ts`，暴露 `convertMarkdownToHTML()`
- ⬜ **M1-7** 类型检查通过，写基础单元测试（Bun test）

**✋ 停止等待确认**：运行测试，确认渲染输出符合预期

---

## M2：packages/db 扩展

**来源**：`woa-layout/src/lib/db/schema.sql` + `db/types.ts`

### 决策说明
- 模板是**平台公共资源**，由管理员管理，无 `client_id` 字段
- 素材文件存本地磁盘（`uploads/`），数据库只存元数据 + 相对路径
- `assets.url` 初期为相对路径，后续迁 CDN 只改这一个字段

### 任务列表

- ⬜ **M2-1** 在 `packages/db/src/schema.ts` 中添加 `templates` 表
- ⬜ **M2-2** 在 `packages/db/src/schema.ts` 中添加 `render_assets` 表
- ⬜ **M2-3** 在 `packages/db/src/queries.ts` 中添加模板 CURD 查询函数
- ⬜ **M2-4** 在 `packages/db/src/queries.ts` 中添加素材 CRUD 查询函数
- ⬜ **M2-5** 运行 `drizzle-kit generate` 生成 migration 文件
- ⬜ **M2-6** 迁移 woa-layout 的两个默认模板（`default-simple`、`overseas-unicorn`）作为种子数据 `packages/db/src/seed.ts`

**✋ 停止等待确认**：检查 migration SQL 和 seed 数据是否正确

---

## M3：api/ — 真实渲染接入

**替换内容**：`api/src/routes/markdown2html.ts` 中的 stub 实现

### 任务列表

- ⬜ **M3-1** 更新 `api/package.json`，添加 `@tentacle-pro/render` 依赖
- ⬜ **M3-2** 重写 `api/src/routes/markdown2html.ts`：从 DB 读取模板，调用 `convertMarkdownToHTML()`
- ⬜ **M3-3** 新增 `api/src/routes/templates.ts`：`GET /templates`、`GET /templates/:id`（只读，管理端单独处理）
- ⬜ **M3-4** 新增 `api/src/routes/assets.ts`：`GET /assets/:filename` 静态文件服务（Bun.file）
- ⬜ **M3-5** 更新 `api/src/index.ts` 挂载新路由
- ⬜ **M3-6** 端到端 curl 测试：`POST /markdown2html` 返回真实 HTML

**✋ 停止等待确认**：用真实 Markdown + templateId 测试，确认 HTML 输出包含内联样式

---

## M4：packages/ui（编辑器 React 组件库）

**来源**：`woa-layout/src/components/`、`src/store/`、`src/hooks/`、`src/lib/`

### 任务列表

- ⬜ **M4-1** 创建 `packages/ui/package.json`，依赖 React 19、shadcn/ui、zustand 等
- ⬜ **M4-2** 迁移 shadcn/ui 基础组件（`button`, `card`, `input`, `select`, `textarea`, `resizable`）
- ⬜ **M4-3** 迁移 `lib/clipboard.ts`（微信剪贴板兼容处理，仅浏览器端）
- ⬜ **M4-4** 迁移 `lib/preset-markdown.ts`、`lib/ui-feedback.ts`、`lib/utils.ts`
- ⬜ **M4-5** 迁移 Zustand store（`store/index.ts`）
- ⬜ **M4-6** 迁移 hooks（`useInitialize.ts`、`useConverter.ts`）
- ⬜ **M4-7** 迁移核心组件：`Layout`、`MarkdownEditor`、`Preview`、`StyleConfigurator`
- ⬜ **M4-8** 将 API base URL 改为可配置（通过 prop 或 env 传入），不硬编码

**✋ 停止等待确认**：在 Storybook 或临时页面中测试组件渲染

---

## M5：web/ 应用

**功能**：
- `tentacle.pro` — 产品首页（静态/SSR）
- `tentacle.pro/dashboard` — 管理员登录后台 Shell
- `tentacle.pro/dashboard/editor` — 嵌入 packages/ui 编辑器

### 任务列表

- ⬜ **M5-1** 创建 `web/package.json`，依赖 Hono、React、`@tentacle-pro/ui`
- ⬜ **M5-2** Bun.serve 入口 + 静态路由（`/*` → index.html SPA）
- ⬜ **M5-3** 首页 HTML/React 页面（产品介绍，3 屏即可）
- ⬜ **M5-4** Dashboard 认证：登录页 + JWT cookie（管理员单账号，凭据在 env）
- ⬜ **M5-5** Dashboard Shell：顶部导航 + 侧边菜单（编辑器、模板管理、素材管理）
- ⬜ **M5-6** 编辑器页：嵌入 `<WoaEditor />` 组件（来自 packages/ui）
- ⬜ **M5-7** 模板管理页：列表 + 创建/编辑/删除（调用 api/ 的 admin 接口）
- ⬜ **M5-8** 素材管理页：上传 + 列表 + 删除（调用 api/ 的 admin 接口）
- ⬜ **M5-9** Nginx 配置示例（`docs/nginx.conf.example`）

**✋ 停止等待确认**：完整流程：登录 → 选模板 → 写 Markdown → 预览 → 复制到微信

---

## 清理

- ⬜ woa-layout 项目归档（加入 README 说明已迁入 backend-tentacle-pro）
- ⬜ backend-tentacle-pro 根目录 README 更新

---

## 注意事项

- 每个阶段结束后等待确认，不提前进入下一阶段
- 素材文件路径约定：`uploads/<filename>`，通过 `GET /assets/:filename` 访问
- 管理端接口（模板/素材 写操作）只对 `web/dashboard` 内部调用开放，不经过公开 API_KEY 鉴权
- `packages/render` 不依赖数据库，模板配置由调用方传入，保持纯函数可测试性
